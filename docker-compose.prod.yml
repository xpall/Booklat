services:
  tunnel:
    image: cloudflare/cloudflared:latest
    container_name: booklat-tunnel
    restart: unless-stopped
    command: tunnel run --token ${CF_TUNNEL_TOKEN}
    network_mode: service:web